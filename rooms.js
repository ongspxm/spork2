const dbase = require('dbase');

const DB_NAME = 'rooms';

function getRooms(from=0){
}

function newRoom(rm){
  // ({user, name, vacancy}) => True
}

function updateRoom(obj){
  // ({user, name, vacancy}) => True
}

function newImg(obj){
  // ({r_id, picData}) => {imgur, link, r_id, dhash}
}

function delImg(obj){
  // ({r_id, imgurId}) => True
}
